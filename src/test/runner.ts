// core file used to run tests in the browser, artifact is at ../debug ( These are not shared on github )

import { Test } from "./test"
import "./test-test"

console.log(`${Test.count} test(s) passed`)